# homebrew-zdx

Homebrew tap for `zdx`.

## Install

```bash
brew tap tallesborges/zdx
brew install zdx
```

## Updating the formula

The release workflow in `tallesborges/zdx` automatically updates `Formula/zdx.rb` on each release.
